/**
 * Api endpoints.
 */
export const API_BACKEND = "backend/api/";
export const API_ENDPOINTS = {
  USERS: `${API_BACKEND}users`,
  BOOKS: `${API_BACKEND}books`,
  ASSIGNS: `${API_BACKEND}assigns`,
};
